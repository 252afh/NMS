@model IEnumerable<NMS.Models.ReportConfigurationNumberViewModels.ReportconfigurationNumber>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FkReportConfiguration)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FkNumber)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FkReportConfiguration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdreportconfigurationNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdreportconfigurationNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdreportconfigurationNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
